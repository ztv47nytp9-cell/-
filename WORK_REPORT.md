# 이력·모바일 입력 개선 결과

작성일: 2026-09-19 / 캐시: m100

## 1. 발견한 기존 문제

- 차량·선박 상세의 최근 운행 기록은 5건으로 제한됐다.
- 선박 전체 이력은 이번 달이 기본값이고, 결과를 모두 한 번에 렌더링했다. 차량에는 전체 이력 진입 버튼이 없었다.
- 입력칸에 포커스하면 본문까지 고정하고 터치 스크롤을 막아 메모를 키보드 위로 올릴 수 없었다.
- 자재 선택 목록에 보관 위치가 없고, 긴 장비 이름·위치는 선택란에서 잘릴 수 있었다.
- 장비 상세는 모든 관리·사용·이동 기록을 한 번에 표시했다.

## 2. 수정한 파일

- app.js: 이력 조회·필터·점진 표시, 입력 화면 높이 대응, 자원 위치 안내, 이송 선택 표시, 기존 선박 필드 보존.
- style.css: 키보드 대응 높이·여백, 긴 이름 줄바꿈, 이력 상세 표시와 터치 영역.
- index.html: 업데이트 안내와 정적 파일 캐시 버전.
- sw.js: 캐시 m100.
- CHANGELOG.md: 변경 내역.
- WORK_REPORT.md: 이 문서.
- 테스트 스크립트는 저장소 밖 outputs/victor-mobile-regression.cjs에 있다.

## 3. 수정한 기능

차량·선박·장비 상세에서 전체 이력을 열고 기간·처리유형·키워드로 검색한다. 출발지·도착지·메모·자원 이름도 키워드 검색 대상이다. 차량·선박 기록 외 정비·연료·연결된 자원 기록도 조회한다. 장비 상세의 기존 수정·삭제 버튼은 더보기를 통해 계속 접근할 수 있다.

## 4. 데이터 구조 변경 여부

저장 키와 데이터 구조, 저장 엔진은 변경하지 않았다. 이력 조회는 원본 배열을 변경하지 않는 읽기 동작이다. 실제 사용자의 저장자료, 서버 자료, Supabase 설정에는 접근하거나 변경하지 않았다.

기존 assetOps 로그·정비·연료 기록, records, equipment의 maintenance·moves를 그대로 읽는다. 선박 기록 수정 시 예전 운항자·동승자·해역·활동횟수·기존 계기값 필드를 삭제하던 코드를 제거해 기존 필드를 보존한다.

## 5. 모바일 키보드 해결 방식

입력 자체를 이유로 본문을 잠그지 않는다. 모달·메뉴가 열릴 때의 배경 잠금은 유지한다.

visualViewport 높이와 offsetTop을 CSS 변수에 반영하고, 포커스·화면 크기 변경 시 가려진 입력란만 필요한 만큼 스크롤한다. 모달은 보이는 영역에 맞춰 높이를 제한하며 textarea 내부 스크롤도 허용한다. 본문은 키보드 공간만큼 아래 여백을 확보한다. 포커스가 해제되면 입력용 여백·하단 메뉴 상태를 복구한다.

기술 참고: [MDN VisualViewport](https://developer.mozilla.org/en-US/docs/Web/API/VisualViewport).

## 6. 이력 조회 변경 방식

- 차량·선박 상세: 최근 10건을 날짜 최신순으로 표시.
- 전체 이력: 기간 제한 없이 시작하며 20건씩 추가 표시.
- 장비 상세: 관리·사용·이동 이력을 각각 최근 10건부터 표시하고 더보기로 20건씩 확장.
- 기간·유형 필터는 접어두고 검색창만 기본 노출.
- 기록 상세를 펼치면 기존 저장 필드와 자원별 정보를 확인.
- 옛 단일 엔진시간 형식도 조회하며, 없는 값은 임의로 생성하지 않음.

## 7. 저장 위치 표시 방식

자재는 기존 stock[보관장소][품목명]과 등록·사건에서 선택한 보관장소를 연결한다. 장비는 기존 equipment.place를 사용한다. 선택 목록과 선택란 아래의 줄바꿈 가능한 안내에 이름·보관장소·수량을 표시한다.

장비 목록은 [보관장소] 이름 순서로 표시한다. 보관장소가 없으면 미지정으로 안내한다. 보관장소를 변경하면 자재 위치·재고 표시를 갱신한다. 기존 자재 저장 구조는 한 기록에 한 출발 보관장소를 쓰므로 이를 임의로 품목별 위치 구조로 바꾸지 않았다.

## 8. 기존 기능 영향 및 검증

격리된 테스트 브라우저와 가상 데이터로 검증했으며 실제 사용자 자료를 사용하지 않았다.

- Chrome 모바일 390×844 및 360×844: 이력 65건의 더보기·검색·기간 필터, 오래된 엔진 기록 조회 통과.
- 이력 조회 전후 원본 상태가 동일한지 확인.
- 자재 위치 변경, 동명 장비의 다른 보관장소 선택, 진행 중 사건의 위치 변경 통과.
- 화면 높이 380px로 키보드를 모의 재현해 본문·모달 메모 입력란이 보이는 범위 안에 있는지 확인.
- 출고 후 재고 80→75, 장비 사용 기록 저장, 재접속 후 유지 통과.
- 차량 기록 저장·수정·삭제 후 계기값 복구 통과.
- 선박 마일·연료를 비운 저장, 수정 시 예전 운항자 필드 보존 통과.
- 장비 관리이력 더보기와 기존 자재 추가창 열기 통과.
- 새 서비스워커 캐시로 오프라인 재접속 및 저장자료 유지 통과.
- JavaScript 문법, manifest 파싱, diff 공백 오류 검사 통과.

입력 포커스·스크롤은 여러 화면에 공통 적용되므로 실제 iOS/Android PWA의 키보드 동작은 실기기에서 추가 확인해야 한다. 브라우저 모의 테스트를 실기기 검증으로 간주하지 않는다.

## 9. 추가 발견한 UX 문제

이송 출발지를 변경하면 장비 선택란 표시와 저장 대상이 달라질 수 있었다. 기존 선택을 그대로 표시하고 출발 보관의 장비로 다시 선택하도록 안내했다. 잘못된 위치 장비의 저장 차단은 기존 로직을 유지했다.

긴 이름은 네이티브 선택창에서 기기마다 표시 폭이 다르므로 선택란 아래에서 전체 이름과 위치를 확인하도록 했다. 선택 UI 전면 교체는 하지 않았다.

## 10. 직접 테스트해야 할 항목

실제 아이폰·안드로이드 PWA에서 메모 선택 → 키보드 표시 → 여러 줄 입력·스크롤 → 키보드 닫기 흐름을 확인해야 한다. 입력창 간 이동과 실제 날짜 선택기도 확인 대상이다.

이 문서의 검증 결과는 로컬 테스트 기준이며, GitHub 업로드 후 실서비스의 배포 완료와 실기기 동작은 별도 확인 대상이다.
